const mdLinks = require('../index.js');

// A. Un test que me compruebe que es una función 
// B. Un test que me compruebe que se esta recibiendo una promesa
// C. 


describe('mdLinks', () => {

  it('should be a function', () => {
    expect(typeof mdLinks).toBe('function');
  });
});
